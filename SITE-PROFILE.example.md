# Site Profile — fill this in for your own site

Copy this file to `SITE-PROFILE.md` in the project/folder you work from, fill it in
once for your site, and keep it updated. All three skills in this kit read it
before doing anything. If a skill can't find this file, it will ask you for these
details instead of guessing or reusing an example site's details.

## Site basics
- Site name:
- Site URL (production):
- Staging URL (if any):
- Niche / topics covered (be specific — this drives the "don't duplicate" and
  topical-authority checks):
- Target audience:
- Default tone (e.g. clear, helpful, beginner-friendly, professional):

## Platform
- SEO plugin: Rank Math / Yoast / SEOPress / other
- Page builder: Elementor (free) / Elementor Pro / other
- Caching / performance plugin (e.g. LiteSpeed Cache, WP Rocket):
- Table plugin used for comparison tables, if any:
- FAQ / Accordion widget used:
- TOC plugin/widget already active (e.g. Easy Table of Contents), so the skill
  never adds a duplicate TOC:

## Editorial defaults
- Default article length range:
- Standing content pillars / categories:
- House style rules you always want followed (e.g. "no em dashes",
  "always 3 title options", "always include a Quick Answer box"):

## WordPress environment facts (confirm before every publish — these change)
- Single-post template name/ID that supplies the public H1:
- Default author name / user ID for new posts:
- Category IDs currently in use:
- Any custom completion-tracking meta key you use to mark a post as
  rewritten/audited (optional — e.g. `_content_rewritten`). Leave blank if
  you don't use one:
- Authorization rule: can this assistant push directly to production, or must
  everything land on staging first for you to review and push yourself?

## Do-not-duplicate list (optional)
List canonical pillar URLs that should never get a competing article without
an explicit decision to consolidate or supersede them.
